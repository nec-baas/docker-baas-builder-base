%% -*- mode: erlang -*-
[
  {rabbit, [
    %% TCP listen port
    {tcp_listeners, [5672]},
    %% Default User
    {default_user, <<"rabbitmq">> },
    %% Default Password
    {default_pass, <<"rabbitmq">> }
  ]}
].
